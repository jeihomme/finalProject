<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="web.dao.face.CalendarDao">
	<select id="schedule" parameterType="Map" resultType="web.dto.CalendarD" >
		SELECT 
			calendarNo, EXTRACT(DAY FROM calendarDate) calendarDate, barNo, bandNo, startTime, endTime
		FROM
			calendar
		WHERE
			calendarDate BETWEEN 
				to_date(#{firstDayOfMonth }, 'DD-MM-YY')
			AND 
				to_date(#{firstDayOfNextMonth }, 'DD-MM-YY')
		ORDER BY
			calendarNo
	</select>
</mapper>